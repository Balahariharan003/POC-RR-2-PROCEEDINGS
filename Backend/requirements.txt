# AI Administrative Co-Pilot (Revenue Recovery Assistant) - Backend Dependencies
# Optimized for Intel i5 CPU / 8GB RAM local deployment

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
python-multipart>=0.0.9

# Document Ingestion & Image Rendering
pymupdf>=1.24.0
pillow>=10.2.0
numpy>=1.26.0

# Local LLM Integration (Ollama qwen2.5:7b)
ollama>=0.2.0

# Document Generation & Templating (.docx)
python-docx>=1.1.0
docxtpl>=0.17.0

# Utilities
requests>=2.31.0

# Optional OCR Extraction Engine (Requires Python <= 3.12 due to onnxruntime wheels)
# On Python 3.10 - 3.12: pip install rapidocr_onnxruntime
# rapidocr_onnxruntime>=1.3.0
